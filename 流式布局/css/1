
/* *{
    box-sizing: border-box;  
     -webkit-box-sizing:border-box;
    /* 点击a标签背景高亮，清除；设置为transparent； */
    -webkit-tap-highlight-color：transprarent ;
    /* 在移动端浏览器默认的外观在ios上加上这个属性才能给按钮和输入框自定义样式 */
    -webkit-appearance：none;
}
img a {/* 禁止长按页面时弹出菜单 */
     -webkit-touch-callout：none；
     } */